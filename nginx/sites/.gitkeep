# Nginx site configs

One file per site (generated by site.sh add), plus default.conf (catch-all)
and site.conf.template (used to generate new configs). Every *.conf here is
served — there's no separate enable/disable step.
